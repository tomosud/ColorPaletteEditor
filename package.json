{
  "name": "color-palette-editor-test",
  "private": true,
  "scripts": {
    "build": "node scripts/build.js",
    "test": "playwright test",
    "test:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.0"
  }
}
